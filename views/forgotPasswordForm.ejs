<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password Form</title>
</head>
<body>
    <div class="container">
        <h1 class="display-3">Forgot Password Form</h1>
        <form action="/api/v1/password/reset/<%= token %>" method="post">
            <input type="password" name="password" placeholder="New password">
            <input type="password" name="confirmPassword" placeholder="Confirm password">
            <button type="submit">Reset password</button>
        </form>
    </div>
</body>
</html> -->

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@100;400&family=Roboto+Mono:ital,wght@1,300&display=swap"
        rel="stylesheet">
    <title>Forgot Password Form - Tripify Andaman</title>
    <style>
        body {
            background-color: #f2f2f2;
            font-family: 'Poppins', sans-serif;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            font-size: 36px;
            font-weight: bold;
            text-align: center;
            margin-bottom: 30px;
            color: #008CBA;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        input {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border-radius: 5px;
            border: none;
            box-shadow: 0px 0px 5px rgba(0, 0, 0, 0.1);
        }
        button{
            font-family: 'Poppins', sans-serif;
        }

        button[type="submit"] {
            background-color: #008CBA;
            color: #ffffff;
            padding: 10px 20px;
            border-radius: 5px;
            border: none;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
        }

        button[type="submit"]:hover {
            background-color: #006b8f;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Forgot Password Form - Tripify Andaman</h1>
        <form action="/api/v1/password/reset/<%= token %>" method="post">
            <input type="password" name="password" placeholder="New password">
            <input type="password" name="confirmPassword" placeholder="Confirm password">
            <button type="submit">Reset password</button>
        </form>
    </div>
</body>

</html>